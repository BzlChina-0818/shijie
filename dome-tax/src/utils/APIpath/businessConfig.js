import {API_BASE} from "./common"
// const API_VAT = API_BASE + '/vat'   //业务配置
/**
 * 业务配置api路径
 */
export default {
  calculateAccruedCfg: `${API_BASE}/biz-prov`,
  define:`${API_BASE}/index/define`
}
